import React from 'react'
import { useContext } from 'react'
import { SiteContext } from '../Context/Context.jsx'

const ProductPreview = () => {
    const { apiData } = useContext(SiteContext)

  return (
    <div>
    </div>
  )
}

export default ProductPreview